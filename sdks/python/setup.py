from setuptools import setup

setup(
    name="agentfirewall",
    version="0.1.0",
    description="Python SDK for AgentFirewall — real-time AI agent security",
    packages=["agentfirewall"],
    python_requires=">=3.10",
)
